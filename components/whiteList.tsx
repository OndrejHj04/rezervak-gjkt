import { Divider, Paper, Typography } from "@mui/material";

export default function WhiteList() {
  return (
    <Paper className="flex flex-col p-2 h-min">
      <Typography variant="h5">Seznam uživatelů</Typography>
      <Divider />
    </Paper>
  );
}
